Jenkins CI/CD Workflow with Docker – Complete Step-by-Step Guide
Objective

Create and configure a Jenkins workflow to:

Pull code from GitHub
Build a Docker image
Run the application
Push the Docker image to Docker Hub automatically using Jenkins Pipeline
Tools Required
Tool	Purpose
Jenkins
	Automation Server
Docker Desktop
	Containerization
Git
	Version Control
GitHub
	Source Code Repository
Docker Hub
	Docker Image Registry
Java JDK 17	Required for Jenkins
System Requirements
Requirement	Recommended
RAM	8 GB
Storage	20 GB Free
OS	Windows 10/11
Internet	Required
Architecture Workflow
Developer Push Code → GitHub Repository
                        ↓
                    Jenkins
                        ↓
                Build Docker Image
                        ↓
               Push to Docker Hub
                        ↓
               Deploy/Run Container
Step 1: Install Java JDK
Download Java

Official Website:

Oracle JDK Download

Install JDK 17.

Verify Installation

Open CMD:

java -version
Expected Output
java version "17.x.x"
Step 2: Install Git
Download Git

Git Download

Install with default settings.

Verify Git
git --version
Expected Output
git version 2.xx.x
Step 3: Install Docker Desktop
Download Docker Desktop

Docker Desktop Download

During Installation Enable:
WSL 2
Virtual Machine Platform

Restart the system after installation.

Verify Docker
docker --version
Expected Output
Docker version xx.x.x
Check Docker Running
docker ps
Expected Output
CONTAINER ID   IMAGE   COMMAND   CREATED   STATUS   PORTS   NAMES
Step 4: Install Jenkins
Download Jenkins

Jenkins Download

Install Jenkins using the Windows installer.

Step 5: Start Jenkins

Open browser:

http://localhost:8080
Step 6: Unlock Jenkins

Open this file:

C:\Program Files\Jenkins\secrets\initialAdminPassword

Copy the password and paste it into Jenkins.

Step 7: Install Suggested Plugins

Click:

Install Suggested Plugins

Wait for installation to complete.

Step 8: Create Jenkins Admin User

Enter:

Username
Password
Email

Click:

Save and Continue
Step 9: Install Docker Pipeline Plugin

Go to:

Manage Jenkins
→ Plugins
→ Available Plugins

Search:

Docker Pipeline

Install plugin and restart Jenkins.

Step 10: Create GitHub Repository

Go to:

GitHub

Create repository:

Docker-Jenkins-App
Step 11: Clone Repository

Open Git Bash:

git clone <repository_url>
Expected Output
Cloning into 'Docker-Jenkins-App'...
Step 12: Create Application Files
Create app.py
from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Jenkins Docker CI/CD Working Successfully"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
Create requirements.txt
flask
Step 13: Create Dockerfile
FROM python:3.10

WORKDIR /app

COPY requirements.txt .

RUN pip install -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["python", "app.py"]
Step 14: Build Docker Image Manually
docker build -t flask-app .
Expected Output
Successfully built <image_id>
Successfully tagged flask-app:latest
Step 15: Run Docker Container
docker run -d -p 5000:5000 flask-app
Step 16: Verify Application

Open browser:

http://localhost:5000
Expected Output
Jenkins Docker CI/CD Working Successfully
Step 17: Push Project to GitHub
git add .
git commit -m "Initial Jenkins Docker Project"
git push origin main
Expected Output
Enumerating objects...
Writing objects...
Step 18: Configure Docker Hub Credentials

Go to Jenkins:

Manage Jenkins
→ Credentials
→ System
→ Global credentials

Add:

Docker Hub Username
Docker Hub Password

ID:

dockerhub-creds
Step 19: Create Jenkins Pipeline Project

Go to:

New Item
→ Pipeline

Project Name:

Docker-Pipeline

Click OK.

Step 20: Configure GitHub Repository

Under Pipeline:

Pipeline script from SCM

SCM:

Git

Repository URL:

<your_github_repository_url>

Branch:

*/main
Step 21: Create Jenkinsfile

Create file named:

Jenkinsfile

Add:

pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "yourdockerhubusername/flask-app"
    }

    stages {

        stage('Clone Repository') {
            steps {
                git branch: 'main',
                url: 'https://github.com/yourusername/Docker-Jenkins-App.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                bat 'docker build -t %DOCKER_IMAGE% .'
            }
        }

        stage('Docker Login') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub-creds',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {

                    bat 'echo %DOCKER_PASS% | docker login -u %DOCKER_USER% --password-stdin'
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                bat 'docker push %DOCKER_IMAGE%'
            }
        }

        stage('Run Container') {
            steps {
                bat 'docker run -d -p 5000:5000 %DOCKER_IMAGE%'
            }
        }
    }
}
Step 22: Push Jenkinsfile to GitHub
git add Jenkinsfile
git commit -m "Added Jenkins Pipeline"
git push origin main
Step 23: Build Jenkins Pipeline

Go to Jenkins:

Build Now
Step 24: Check Build Console Output

Click:

Console Output
Expected Output
Started by user admin
Cloning repository...
Building Docker image...
Login Succeeded
Pushing image...
Successfully pushed image
Finished: SUCCESS
Step 25: Verify Docker Image on Docker Hub

Open:

Docker Hub

Expected Output
flask-app image available
Step 26: Verify Running Container

CMD:

docker ps
Expected Output
CONTAINER ID   IMAGE                         STATUS
xxxxxxx        yourdockerhub/flask-app      Up
Step 27: Access Final Application

Open browser:

http://localhost:5000
Final Output
Jenkins Docker CI/CD Working Successfully
Important Jenkins Commands
Restart Jenkins
net stop jenkins
net start jenkins
Check Running Containers
docker ps
Stop Container
docker stop <container_id>
Remove Container
docker rm <container_id>
Remove Docker Image
docker rmi <image_name>
Common Errors and Solutions
Error	Solution
Docker daemon not running	Start Docker Desktop
Jenkins localhost:8080 not opening	Restart Jenkins service
Permission denied Docker	Add Jenkins user to Docker group
docker command not found	Add Docker to PATH
Git not recognized	Reinstall Git
Pipeline failed	Check Console Output
Advantages of Jenkins CI/CD
Automated build process
Faster deployment
Continuous Integration
Easy Docker integration
Reduced manual work
Scalable workflows
Conclusion

Successfully configured:

Jenkins Automation Server
Docker Integration
GitHub Repository
CI/CD Pipeline
Docker Image Push Workflow

The Jenkins pipeline now automatically:

Pulls source code from GitHub
Builds Docker image
Pushes image to Docker Hub
Runs application container automatically
